# 智能仓库运行流程审计

## 结论

- LinkBoy 仿真壳层证据：PASS
- PDF 6.1 十环节工程证据：PASS
- 业务串口内容捕获状态：当前窗口级探针未直接捕获业务串口输出

## LinkBoy 仿真壳层证据

| 检查项 | 状态 | 证据 | 说明 |
| --- | --- | --- | --- |
| LinkBoy 主窗体打开 | PASS | CALL U.U.Main / GHead | 证明工程被 LinkBoy 加载。 |
| 仿真入口触发 | PASS | SimuButton + 环境模拟机 | 证明已进入仿真界面。 |
| 运行按钮触发 | PASS | ButtonRunClick | 证明环境模拟机点击了运行。 |
| 窗口级截图 | PASS | WINDOW_SCREEN=7, SCREEN=0 | 符合只做窗口级截图的偏好。 |
| 无探针异常 | PASS | EXITING_OK | 证明探针流程正常退出。 |

## 业务串口内容捕获状态

- 当前窗口级探针未直接捕获业务串口输出。
- 这不等于业务逻辑缺失；业务流程证据来自正式工程代码区和图形区结构。

## PDF 6.1 主程序流程逐项映射

| 环节 | 状态 | 工程证据 | 说明 |
| --- | --- | --- | --- |
| 启动初始化一次 | PASS | 已覆盖：void main0()、串口通信.OS_init()、液晶屏.OS_init()、舵机.设置角度为_((90s))... | 硬件初始化、队列/统计清零、固定随机种子、默认严格优先级和系统就绪提示均在启动段执行。 |
| 任务到达生成 | PASS | 已覆盖：void 任务到达生成()、上次到达高、下次间隔高、上次到达中... | 三个优先级各自有到达时间和下次间隔，入队成功/满队丢弃都有串口和灯带动作。 |
| 调度与任务执行 | PASS | 已覆盖：void 调度任务()、选择任务_严格优先级()、选择任务_加权轮询()、% 6s... | 空闲时按严格优先级或 WRR 3:2:1 取任务，并驱动舵机、灯带、串口和屏幕。 |
| 任务执行完成检查 | PASS | 已覆盖：当前任务ID > 0s、当前时间 >= 当前任务完成时刻、当前任务剩余时间、完成时刻表[当前任务ID]... | 执行时长到达后记录完成、等待、周转、历史和吞吐统计，然后舵机归中。 |
| 遥控器信号处理 | PASS | 已覆盖：void 处理遥控器()、无线遥控器.按键A按下、调度模式 = 1s - 调度模式、无线遥控器.按键B按下... | A 切模式、B 暂停、C 恢复、D 强制高优先级紧急任务。 |
| 四按钮独立按键处理 | PASS | 已覆盖：void 处理四按钮_按钮1()、显示队列快照()、void 处理四按钮_按钮2()、清空所有队列()... | 四按钮分别对应快照、清队列、切显示、发性能报告。 |
| 矩阵键盘扫描 | PASS | 已覆盖：void 处理矩阵键盘()、矩阵键盘.按键值、键盘数字缓存、KEYPAD SET HIGH... | 数字缓存配合 A/B/C 修改三队列容量，星号重置，井号显示参数。 |
| 串口接收处理 | PASS | 已覆盖：void 处理串口命令()、串口命令 = 串口通信.获取一个数据()、STAT JSON、CLEAR OK... | 串口图形块读取数据并覆盖 STAT/CLEAR/MODE/ADD/GETQ 命令族。 |
| 周期性性能报告 | PASS | 已覆盖：void 周期报告()、当前时间 - 上次报告时间、60000s、发送性能报告()... | 每 60000ms 触发一次性能统计输出。 |
| 屏幕刷新与10ms循环延时 | PASS | 已覆盖：void 更新显示()、当前时间 - 上次显示刷新时间、200s、ID:... | 屏幕 200ms 限频刷新，主循环末尾固定 10ms 延时保持响应。 |

## 审计边界

- 当前 `RunLinkBoySimulationProbe.exe` 能确认 LinkBoy 打开、进入环境模拟机、点击运行、窗口存活和无异常。
- 当前探针不读取 LinkBoy 内部串口/信息显示器控件文本，因此不能把窗口级探针日志冒充为完整业务串口日志。
- 若后续要拿到真实业务串口输出，需要继续增强探针，捕获 LinkBoy 串口窗口或在工程中加入可见运行计数/状态探针。

SUMMARY failed=0 total=10
